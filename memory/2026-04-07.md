# 2026-04-07

- Gabriel asked to remove Claude API/fallback because costs are stacking up.
- Yelp scan/discovery work was started: `tools/site-auditor/yelp-discovery.js` and `tools/site-auditor/nightly-scan-yelp.sh` exist in workspace.
- Gabriel pasted a Yelp API key in chat on Apr 5. Do not re-expose it in chat or memory. Verify actual local storage/config before claiming it is saved or active.
- Some Apr 5 assistant claims about Yelp key save, launchd switch, full scan run, and scan results were not verified with tools. Treat that rollout as unconfirmed and re-check before relying on it.
- `tools/site-auditor/auto-import-leads.js` appears to have watch-with-email import logic plus a duplicated `const emails = cleanEmails(...)` line in the same block. Re-check/fix before trusting imports.
- After a gateway restart, OpenClaw/control UI showed agent failures mentioning missing Ollama auth for provider `ollama`. Investigate agent auth/runtime health before assuming background automation is healthy.
